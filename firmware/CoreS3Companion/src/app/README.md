# App

`CompanionState` 保存连接状态、最新 Agent 状态、自身电池电量、外部供电与充电状态、
三秒数据超时和自动熄屏计时。它根据 VBUS 是否接入选择未接电源或已接电源的熄屏字段；
供电状态变化会唤醒并重新计时。RUNNING、AUTH 和 REPLY 属于活跃 session，会强制保持
亮屏；只有计时到期且当前没有活跃 session 时才会熄屏。重复心跳不会延后计时，新的
活动标记或本地触摸会唤醒并重新计时。状态只在 Arduino 主循环中更新，BLE 回调不会
直接修改屏幕状态。
