# Protocol

定义 BLE UUID、协议版本和 Agent 状态帧编码器。Agent 帧包含运行状态、来源、5H/Weekly
剩余额度、Context 占用、额度重置剩余分钟数、最多 60 字节的 UTF-8 显示标题，以及
模型名称、effort、未接电源/已接电源熄屏分钟数和最近一次 Agent 活动标记。

客户端编码器与 CoreS3 固件按 Release 配套演进，不进行跨版本能力协商。正式运行时两端
必须来自同一个 Release 标签；升级时应同时更新客户端和固件。

三字节 CPU 编码器只为第一版协议兼容和回归测试保留，新客户端不再发送该消息。
